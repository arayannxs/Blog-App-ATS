var getNative = require('./_getNative'),
    ***REDACTED*** = require('./_***REDACTED***');

/* Built-in method references that are verified to be native. */
var WeakMap = getNative(***REDACTED***, 'WeakMap');

module.exports = WeakMap;
